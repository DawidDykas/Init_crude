# exceptions/service_exception.py


class ServiceException(Exception):
    """
    Custom application exception.

    Used in service layer to raise errors
    independently from Flask/FastAPI.
    """

    def __init__(
        self,
        status_code: int,
        detail: str
    ):
        self.status_code = status_code
        self.detail = detail

        super().__init__(detail)